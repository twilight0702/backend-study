---
文档链接: https://jwt.io/introduction
---
# 什么是JWT

## 定义

JSON Web Token（JWT）是一种开放标准（[RFC 7519](https://tools.ietf.org/html/rfc7519)），定义了一种紧凑且自包含的方式，用于在各方之间安全地传输信息，形式为 JSON 对象。由于该信息经过数字签名，因此可以验证并信任其真实性。JWT 可以使用密钥（通过 **HMAC 算法** ）或公钥/私钥对（ **使用 R** **SA 或 ECDSA**）进行签名。

## 使用场景

- **登录授权**：用户登录后，后续的每个请求都会包含 JWT，从而允许用户访问该令牌授权的路线、服务和资源。
- **信息交换**：JSON Web Tokens（JWT）是安全传输信息的一种有效方式。由于 JWT 可以进行签名（例如使用公钥/私钥对），您可以确信发送者就是其声称的身份。此外，由于签名是通过 Head 和 Payload 计算得出，您还可以验证内容是否被篡改。

## 结构

在紧凑形式中，JSON Web Token 由三个部分组成，各部分之间用点 (`.`) 分隔，具体包括：

- Header  标题
	- 令牌类型 + 使用的算法
- Payload  有效载荷
- Signature  签名

```
xxx.yyy.zzz
```

但是是无状态授权机制，后续可以加入redis缓存实现过期

还有自动登录