# 模板代码（双重锁DCL）

```java
public class Singleton {
    // 1. 使用 volatile 禁止指令重排序，确保线程安全
    private static volatile Singleton instance;

    // 2. 私有构造函数，禁止外部 new
    private Singleton() {}

    // 3. 提供全局唯一访问入口
    public static Singleton getInstance() {
        // 第一次检查，避免每次都加锁
        if (instance == null) {
            // 多线程环境下加锁，避免重复创建
            synchronized(Singleton.class) {
                // 第二次检查，确保当前线程拿到锁后 instance 仍未初始化
                if (instance == null) {
                    instance = new Singleton();
                }
            }
        }
        return instance;
    }
}
```

### 🔹 Q1：什么是单例模式？用在哪些场景？

- 单例：保证类全局只有一个实例。
    
- 应用场景：数据库连接池、线程池、日志系统、配置中心、注册表对象等。

---

### 🔹 Q2：什么是懒汉/饿汉？区别？

|模式|是否懒加载|是否线程安全|特点|
|---|---|---|---|
|饿汉|❌|✅|类加载即创建，不适合耗资源对象|
|懒汉|✅|❌（默认）|延迟加载但线程不安全|
|DCL|✅|✅|推荐，兼顾效率与线程安全|

---

### 🔹 Q3：不加 `volatile` 会出现什么问题？为什么？

如上所述，会因**指令重排**造成其他线程拿到**未初始化完成**的对象，严重时会产生 `NullPointerException` 或状态异常。

---

### 🔹 Q4：synchronized 加在方法上 vs 加在代码块上，有什么区别？

- 加在方法上：锁的是整个方法，粒度大，影响性能
    
- 加在代码块上（如 `synchronized(Singleton.class)`）：更细粒度控制，配合 DCL 提升性能
