spring:
  datasource:
    url: jdbc:mysql://localhost:3306/atguigu?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: root
    password: YUyue0702
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      lettuce:
        pool:
          max-active: 8 # 连接池最大连接数
          max-idle: 8 # 连接池最大空闲连接数
          min-idle: 0 # 连接池最小空闲连接数
          max-wait: 100 # 连接池最大等待时间
