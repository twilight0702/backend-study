
## **1. JavaScript**

- **主要是即时解释（Just-In-Time + Interpreter）**
    - 浏览器（V8、SpiderMonkey 等）会直接读取 JS 源代码（文本）
    - 先**快速解释执行**，让程序立刻运行
    - 同时在后台 **JIT 编译热点代码**，提高性能
- **特点**：
    - 修改后刷新页面就生效，不需要单独编译步骤
    - 依赖运行环境（浏览器、Node.js）内置的解释器和 JIT
- **跨层含义**：
    - 你的 `.js` 文件被当作 UTF-8 文本读入内存
    - 解析成抽象语法树（AST）
    - 解释器逐行执行，并可在运行中替换为机器码

---

## **2. Python**

- **严格来说不是纯解释执行**，而是**解释 + 字节码虚拟机**：
    - `.py` 文件是文本
    - **第一次运行时**，Python 解释器（CPython）会编译成字节码（`.pyc`），存到 `__pycache__` 目录
    - 字节码在 **Python 虚拟机（PVM）** 上解释执行
- **特点**：
    - 不需要手动编译，但实际上还是有编译这一步（自动发生）
    - 修改 `.py` 后，再次运行会重新编译（或用缓存）
- **跨层含义**：
    - `.py` → 解析成 AST → 编译成 `.pyc` 字节码 → Python 虚拟机解释字节码 → OS 调用

---

## **3. 和 Java 对比**

[[Java编译运行过程]]

| 语言     | 编译时机        | 中间形式            | 执行方式       | 热更新可能性          |
| ------ | ----------- | --------------- | ---------- | --------------- |
| Java   | 显式编译（javac） | JVM 字节码（.class） | JVM 解释+JIT | 需重启 JVM 或用动态类加载 |
| JS     | 运行时即时解析     | AST / 内存字节码     | 解释+JIT     | 高，刷新/热替换即可      |
| Python | 运行时自动编译     | `.pyc` 字节码      | 解释执行       | 高，重新运行文件即可      |

---

也就是说：

- JS 的“修改立刻生效”是因为它是**源代码级解释**（每次运行都读文本）
    
- Python 虽然有字节码，但编译是自动触发的，看起来也是“即改即生效”
    
- Java 因为编译是独立步骤，所以必须 **重新编译 + 重新运行** 才能看到效果（除非用动态加载、热替换技术）

# Rust / C++

