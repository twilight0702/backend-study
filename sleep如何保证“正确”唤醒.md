---
简介: 和ghk关于sleep的讨论
---
# 1. `Thread.sleep()` 的本质

- 让**当前线程**进入 **TIMED_WAITING（超时等待）** 状态，挂起至少指定时间。
- 由 **JVM 发起**，通过系统调用让 **操作系统调度器**将线程移出运行队列。
- 到期后线程进入 **RUNNABLE（就绪态）**，等待再次分配 CPU。
- **不会释放锁**。

---

# 2. 为什么它不等于“唤醒”

- **至少等待 n 毫秒**，到期后只是进入就绪态，不保证立即运行。
- 受操作系统调度影响，实际执行可能延迟。
- **非事件驱动**，不关心外部条件是否满足。
- 可被 `Thread.interrupt()` 提前打断。

---

# 3. 真正的“等待-唤醒”机制

- `wait()` / `notify()`：配合同步锁，基于对象监视器的条件等待。
- `LockSupport.park()` / `unpark()`：底层同步工具，不依赖锁。
- `Condition.await()` / `signal()`：基于 `Lock` 的条件变量。
- 适合等待具体事件发生，能精确控制唤醒时机。

---

# 4. 为什么 `sleep` 看起来很准

- 大多数情况系统负载低、调度延迟小（1~2ms），所以看似准确。
- 但本质上只是 OS **尽快**调度，而非保证精确。
- 高负载、低优先级时延迟会明显增加。

---
# 5. 线程状态切换控制者

- **RUNNING → TIMED_WAITING（或睡眠态）**：线程调用系统调用（如 Linux 的 `nanosleep()`、Windows 的 `Sleep()`），向操作系统请求挂起自己一定时间，操作系统将线程从运行队列中移除，置于睡眠队列或定时等待队列中。
- **TIMED_WAITING → RUNNABLE（就绪态）**：操作系统定时器中断触发，检查睡眠队列中已到时间的线程，将它们状态标记为就绪，放入就绪队列，等待 CPU 调度。
- **RUNNABLE → RUNNING（运行态）**：操作系统调度器根据调度算法从就绪队列中选择线程分配 CPU，线程获得时间片开始执行。

---

# 6. 定时器中断与就绪队列

- OS 维护**定时等待队列**，定时器中断检查到期线程。
- 到期线程标记为 RUNNABLE，插入**就绪队列**。
- 是否立即执行取决于调度器策略（如 Linux CFS 的 vruntime 算法），与 Java `Thread` 优先级关系不大。

---

# 7. 最佳实践

- **不要用 `sleep` 做同步等待**，仅适用于延迟、轮询等简单场景。
- 等待事件时应使用 `wait/notify`、`Condition`、`CountDownLatch`、`Future.get()` 等同步工具。
- 需要高精度定时可用 `nanoTime + 自旋`（但会占 CPU）。
