---
简介: 面试八股
---
# 1 运行时内存结构

## 1.1 概述

包括以下部分：
- 程序计数器
- Java虚拟机栈
- 本地方法栈
- 堆
- 方法区
- 运行时常量池

程序计数、堆、栈（虚拟机栈、本地方法栈）、元空间（+运行时常量池）
## 1.2 详细解释

### 1.2.1 程序计数器

- 线程私有
- 记录下一条要执行的JVM指令地址
- 唯一一个不会发生 `OutOfMemoryError` 的区域
### 1.2.2 Java虚拟机栈

- 线程私有
- 每一个方法在执行的时候会创建一个**栈帧**，用于存储局部变量、操作数栈、动态链接、方法返回地址等
- 出现栈深度过大或无限递归，会抛出 `StackOverflowError`。
- 如果申请内存失败，抛出 `OutOfMemoryError`。
### 1.2.3 本地方法栈

- 和虚拟机栈类似，但是服务于JVM使用的Native方法
- <span style="background:rgba(5, 117, 197, 0.2)">HotSpot JVM 实现中，虚拟机栈与本地方法栈通常是合并实现的</span>

### 1.2.4 堆

- ==所有线程共享，是JVM中最大的一块内存区域==
- 存储所有对象实例和数组，是垃圾回收的主要区域
- 划分：
	- 新生代
	- 老年代
	- 元空间（<span style="background:rgba(5, 117, 197, 0.2)">存在于本地内存中</span>）（Java8以前叫永久代）
### 1.2.5 方法区（元空间）

- ==所有线程共享==
- <span style="background:rgba(5, 117, 197, 0.2)">存储已被虚拟机加载的类信息、常量、静态变量、JIT编译后的代码等</span>
<span style="background:rgba(5, 117, 197, 0.2)">- Java 8 被元空间（Metaspace）取代，**分配在本地内存中，而非虚拟机内存**</span>
### 1.2.6 运行时常量池

- 是方法区的一部分，用于存放编译期生成的各种字面量和符号引
- 具有动态性，运行时也可将新的常量放入池中。当无法申请到足够内存时，会抛出 OutOfMemoryError 异常

## 堆栈的区别

|      | 堆                          | 栈                                    |
| ---- | -------------------------- | ------------------------------------ |
| 用途   | 存储对象的实例（包括类的实例和数组），new锁创建的 | - 局部变量、方法调用的参数、方法返回地址以及一些临时数据。**栈帧** |
| 生命周期 | 由GC回收                      | 方法调用结束就会销毁栈帧                         |
| 存取速度 | 慢，对象在堆上的分配回收需要更多时间         | 快，先进后处，操作简单快速                        |
| 存储空间 | 较大，动态扩展                    | 小，固定                                 |
| 可见性  | 线程共享                       | 线程私有                                 |
栈中保存对象的引用，实际对象存储在堆中
# 2 Java内存模型JMM
