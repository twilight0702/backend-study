---
简介: 前后端部署模式及涉及的技术栈解析
---
## **一、前后端分离部署模式**

### 1. 常见部署方式

- **前端部署在 Nginx 上，后端部署在 Tomcat（或其他应用服务器）**
    
    - Nginx：托管前端静态资源（HTML、CSS、JS），处理反向代理。
        
    - Tomcat：运行 Java Web 应用，处理 API 请求。
        
- 优点：性能高、职责分离、可扩展。
    

### 2. 访问流程

```
浏览器 → Nginx（静态资源 & 反向代理） → 后端应用服务器（Tomcat）
```

---

## **二、Web 服务器 vs Java 应用服务器**

### 1. Web 服务器（Web Server）

- **作用**：接收 HTTP 请求，返回静态内容（HTML、CSS、JS、图片）。
    
- **例子**：Nginx、Apache HTTP Server、IIS。
    
- **特点**：高性能处理静态资源，可反向代理。
    

### 2. Java 应用服务器（Java Application Server）

- **作用**：运行 Java Web 应用（Servlet、JSP、Spring MVC、Spring Boot），生成动态内容。
    
- **例子**：Tomcat、Jetty、WildFly、GlassFish。
    
- **特点**：提供 Java EE 相关规范支持，能运行 Java 代码。
    

### 3. 区别

|对比项|Web 服务器|Java 应用服务器|
|---|---|---|
|主要职责|静态资源、HTTP 请求转发|运行 Java Web 应用|
|支持语言|通用|Java|
|性能|静态快|动态处理能力强|
|常用产品|Nginx、Apache|Tomcat、Jetty|

---

## **三、Java 应用服务器相关技术**

### 1. Java Web 应用核心技术

- **Servlet**：处理 HTTP 请求的 Java 类（Java EE 标准）。
    
- **JSP**：HTML + Java 代码（早期常用）。
    
- **Spring MVC / Spring Boot**：基于 Servlet 的现代框架。
    

### 2. Java EE / Jakarta EE 规范

- **Servlet API**：Web 请求处理标准。
    
- **JSP**：动态 HTML 生成标准。
    
- **JNDI**：统一的资源查找接口。
    
- **JTA**：事务管理标准。
    
- **EJB**：企业级组件标准（较少用）。
    

### 3. 常见 Java 应用服务器产品

- **Tomcat**：轻量级 Servlet 容器。
    
- **Jetty**：可嵌入的轻量容器。
    
- **WildFly / JBoss**：全功能 Java EE 服务器。
    
- **GlassFish**：Java EE 官方参考实现。
    

---

## **四、Java 标准接口 + 厂商实现 + Spring 封装 模式**

### 1. Web 层

- **标准**：Servlet API（Java 官方）
    
- **实现**：Tomcat、Jetty
    
- **Spring 封装**：DispatcherServlet（Spring MVC）
    

### 2. 数据库层

- **标准**：JDBC API（Java 官方）
    
- **实现**：各数据库厂商的 JDBC 驱动（MySQL、PostgreSQL、Oracle）
    
- **Spring 封装**：JdbcTemplate、Spring Data JPA、MyBatis
    

### 3. 模式总结

```
Java 标准接口  →  第三方实现  →  Spring 封装
```

这种模式的好处是：

- 开发只依赖标准接口，不绑死某个厂商实现。
    
- 切换底层实现时，业务代码改动小。
    
- Spring 帮忙减少样板代码，提升开发效率。
    
