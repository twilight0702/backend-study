---
简介: 前后端部署模式及涉及的技术栈解析
---
# 1 前后端分离部署模式

## 1.1 常见部署方式

- **前端部署在 Nginx 上，后端部署在 Tomcat（或其他应用服务器）**
    
    - Nginx：托管前端静态资源（HTML、CSS、JS），处理反向代理。
        
    - Tomcat：运行 Java Web 应用，处理 API 请求。
        
- 优点：性能高、职责分离、可扩展。

## 1.2 访问流程

```
浏览器 → Nginx（静态资源 & 反向代理） → 后端应用服务器（Tomcat）
```

---

# 2 后端部分

> 关于[[Springboot和tomcat#3 我的代码和tomcat的关系？|我的代码和tomcat的关系]]
## 2.1 Web 服务器 vs Java 应用服务器

### 2.1.1 Web 服务器（Web Server）

- **作用**：接收 HTTP 请求，返回静态内容（HTML、CSS、JS、图片）。
    
- **例子**：Nginx、Apache HTTP Server、IIS。
    
- **特点**：高性能处理静态资源，可反向代理。
    

### 2.1.2 Java 应用服务器（Java Application Server）

- **作用**：运行 Java Web 应用（Servlet、JSP、Spring MVC、Spring Boot），生成动态内容。
    
- **例子**：Tomcat、Jetty、WildFly、GlassFish。
    
- **特点**：提供 Java EE 相关规范支持，能运行 Java 代码。
    

### 2.1.3 区别

|对比项|Web 服务器|Java 应用服务器|
|---|---|---|
|主要职责|静态资源、HTTP 请求转发|运行 Java Web 应用|
|支持语言|通用|Java|
|性能|静态快|动态处理能力强|
|常用产品|Nginx、Apache|Tomcat、Jetty|

---

## 2.2 Java 应用服务器相关技术

### 2.2.1 Java Web 应用核心技术

- **Servlet**：处理 HTTP 请求的 Java 类（Java EE 标准）。
    
- **JSP**：HTML + Java 代码（早期常用）。
    
- **Spring MVC / Spring Boot**：基于 Servlet 的现代框架。
    

### 2.2.2 Java EE / Jakarta EE 规范

- **Servlet API**：Web 请求处理标准。
    
- **JSP**：动态 HTML 生成标准。
    
- **JNDI**：统一的资源查找接口。
    
- **JTA**：事务管理标准。
    
- **EJB**：企业级组件标准（较少用）。
    

### 2.2.3 常见 Java 应用服务器产品

- **Tomcat**：轻量级 Servlet 容器。
    
- **Jetty**：可嵌入的轻量容器。
    
- **WildFly / JBoss**：全功能 Java EE 服务器。
    
- **GlassFish**：Java EE 官方参考实现。
    

---

## 2.3 Java 标准接口 + 厂商实现 + Spring 封装 模式

### 2.3.1 Web 层

- **标准**：Servlet API（Java 官方）
    
- **实现**：Tomcat、Jetty
    
- **Spring 封装**：DispatcherServlet（Spring MVC）
    

### 2.3.2 数据库层

- **标准**：JDBC API（Java 官方）
    
- **实现**：各数据库厂商的 JDBC 驱动（MySQL、PostgreSQL、Oracle）
    
- **Spring 封装**：JdbcTemplate、Spring Data JPA、MyBatis
    

### 2.3.3 模式总结

```
Java 标准接口  →  第三方实现  →  Spring 封装
```

这种模式的好处是：

- 开发只依赖标准接口，不绑死某个厂商实现。
    
- 切换底层实现时，业务代码改动小。
    
- Spring 帮忙减少样板代码，提升开发效率。
    
# 3 前端部分

## 3.1 前端代码是怎么运行起来的？

> 前端的运行模式是：我把前端（比如说用vue）开发好，然后打包成静态的三件套代码，放到nginx中，当用户访问我的服务器（域名等）的时候，我把这静态网页的代码发送给他，然后在他本地的浏览器运行，有后端请求就发送给nginx，nginx再代理到我后端地方进行请求
### 3.1.1 开发阶段

- 你用 Vue、React 或其他框架写的前端代码，本质上是 **HTML + CSS + JavaScript** 的源代码。
- 打包构建（`npm run build`）后，得到一堆静态文件：
    
    ```
    dist/
    ├── index.html
    ├── js/
    ├── css/
    ├── assets/
    ```
    
- 这些文件**不需要 Node.js 运行**，它们只是纯静态资源，浏览器就能执行。

---

### 3.1.2 部署到 Nginx

- 把打包后的静态文件放到 Nginx 配置的 `root` 目录，比如：
    
    ```
    /var/www/html/myapp
    ```
    
- Nginx 配置两个主要规则：
    1. **静态资源直接返回**（HTML、CSS、JS、图片）
    2. **API 请求反向代理**到后端 Spring Boot
    
    例子：
    ```nginx
    server {
        listen 80;
        server_name example.com;
    
        # 静态前端
        location / {
            root /var/www/html/myapp;
            index index.html;
        }
    
        # API 代理
        location /api/ {
            proxy_pass http://localhost:8080/;
        }
    }
    ```
    

---
### 3.1.3 用户访问时的运行流程

假设用户访问 `https://example.com`：

1. **浏览器发请求**
    
    - 浏览器向 `example.com` 请求 `/`
    - Nginx 返回 `index.html` 文件给浏览器。
        
2. **浏览器本地运行前端代码**
    
    - 浏览器解析 HTML → 加载 CSS、JS 文件
    - JS 开始运行，负责渲染页面和监听用户操作。
        
3. **需要后端数据时**
    
    - 前端 JS 调用 `fetch('/api/user/list')`
    - 浏览器发 HTTP 请求到 `example.com/api/user/list`
        
4. **Nginx 处理 API 请求**
    
    - 看到 `/api/` 路径 → 反向代理到 `http://localhost:8080/api/user/list`（你的 Spring Boot 后端）
        
5. **后端返回数据**
    
    - Spring Boot 处理业务逻辑，查数据库，返回 JSON
    - Nginx 把 JSON 转发回浏览器。
        
6. **前端渲染数据**
    
    - 浏览器 JS 解析 JSON
    - 更新页面 DOM，把数据展示出来。

---

### 3.1.4 关键点

- ==**前端代码运行环境**：在用户本地浏览器，不在服务器上执行。==
- **Nginx 角色**：
    
    - ==作为静态文件服务器（发 HTML/CSS/JS）==
        
    - ==作为反向代理（把 API 请求转到后端）==
        
- **用户隔离性**：==每个用户拿到的静态文件是一样的，运行在各自的浏览器里，互不干扰。==

## 3.2 关于开发阶段的“run”？

> 在开发 Vue 网页的时候需要用命令行 `npm run serve`（或 `vite dev` 之类）启动本地端口，其实是因为**开发阶段和部署阶段的运行方式不一样**。

### 3.2.1 部署阶段（生产环境）

- 前端代码打包成静态文件（HTML/CSS/JS） → 放到 Nginx → 用户浏览器直接访问。
    
- 没有任何“运行前端代码”的服务器逻辑，Nginx 只是文件分发器。
    
- 这是最终上线给用户访问的模式。
### 3.2.2 开发阶段（本地调试）

- 你写的 Vue 代码不是最终浏览器能直接运行的形式，比如：
    
    - `.vue` 单文件组件浏览器不认识
        
    - ES6+、TypeScript 需要编译
        
    - CSS 可能用 Less/Sass，需要预处理
        
- 所以**必须有一个工具帮你实时编译 + 热更新**，这就是 `npm run serve`（Vue CLI）或 `vite` 的作用。
    

### 3.2.3 开发服务器做了几件事：

1. **实时编译打包**
    
    - 把 `.vue`、`ts` 等源码编译成浏览器能跑的 JS/CSS。
        
2. **热更新（HMR）**
    
    - 你改了代码，它不会重新加载整个页面，而是只替换改动的模块，方便调试。
        
3. **本地 HTTP 服务**
    
    - 启一个 Node.js 进程监听本地端口（如 5173、8080），你访问 `http://localhost:8080` 就能看到最新编译结果。
        
4. **代理后端 API（解决跨域）**
    
    - 开发时可能需要访问后端 API，它会帮你把 `/api` 转发到后端地址（相当于临时的小型 Nginx）。
        

---

### 3.2.4 为什么生产环境不需要 `npm run serve`

- 因为生产环境不需要**实时编译、热更新、跨域代理**这些功能。
    
- 你会先用 `npm run build` 生成**最终静态文件**，直接交给 Nginx 或其他静态服务器就能跑，不需要 Node.js 开发服务器。
    

---

### 3.2.5 对比总结

|阶段|运行方式|作用|
|---|---|---|
|**开发阶段**|`npm run serve`（Node.js 本地开发服务器）|实时编译 + 热更新 + 跨域代理|
|**部署阶段**|`npm run build` → 静态文件 → Nginx|提供最终代码给用户，浏览器直接运行|

所以简单说：

> 你本地 `npm run serve` 的端口是**开发服务器**，方便调试；  
> 用户访问 Nginx 的端口才是**生产环境服务器**，跑的是打包后的静态文件。

## 3.3 Nginx的作用

### 3.3.1 静态资源服务器

- **作用**：直接把 HTML、CSS、JS、图片等文件返回给浏览器。
    
- **场景**：你打包好的 Vue 项目（`index.html` + js/css）放到 Nginx 的 `root` 目录，用户访问时直接返回这些文件。
    
- **好处**：
    
    - 性能高（Nginx 基于事件驱动，高并发能力很强）
        
    - 可以做缓存（减少文件读取开销）
        
    - 能直接处理大量静态文件请求，不占用后端资源
        

---

### 3.3.2 反向代理（代理 API 请求）

- **作用**：浏览器访问 Nginx，由 Nginx转发到后端 Spring Boot 服务。
    
- **场景**：
    
    - 浏览器请求 `https://example.com/api/user`
        
    - Nginx 把请求转发给 `http://localhost:8080/api/user`（Spring Boot）
        
- **好处**：
    
    - 隐藏后端真实地址（安全性）
        
    - 避免跨域问题（同域代理）
        
    - 可以统一管理多个后端服务的入口（API 网关的雏形）
        

---

### 3.3.3 负载均衡

- **作用**：当你有多个后端实例时，Nginx 可以按一定策略把请求分配给不同后端。
    
- **场景**：
    
    ```
    upstream backend {
        server 192.168.0.101:8080;
        server 192.168.0.102:8080;
    }
    location /api/ {
        proxy_pass http://backend;
    }
    ```
    
- **好处**：
    
    - 提高系统并发能力
        
    - 某个后端挂了可以自动切换到其他节点
        

---

### 3.3.4 安全与访问控制

- HTTPS 证书部署（TLS/SSL 终止点）
    
- 限流、防盗链、防爬虫
    
- IP 访问限制
    

---

### 3.3.5 在你的场景里的位置

结合你的 Vue 前端 + Spring Boot 后端：

```
[ 用户浏览器 ]
       ↓ 访问 example.com
[ Nginx ]
 ├── 静态资源路径 /        → 返回 index.html/js/css
 ├── API 代理路径 /api/   → 转发到 Spring Boot
 └── WebSocket 路径 /ws/  → 转发到 Spring Boot
```

也就是说：

- 用户访问前端页面，Nginx 直接返回静态文件
    
- 用户点击按钮触发 API 请求，Nginx 把请求代理到 Spring Boot
    
- 如果有实时功能（WebSocket），Nginx 也能帮你转发长连接