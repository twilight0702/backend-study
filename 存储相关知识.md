---
时间: 2025.8.11
简介: 对于各种层级的存储相关知识整理
---
> 起因是在同时学习不同层级的存储相关，知识体系比较混乱，因此整理汇总，纵向串联。
> 语言层级方面本文着重于Java，其余语言不太了解，互相之间应该还是会有区别

# 1 存储的全景分层

（从物理硬件 → 操作系统 → 编程语言 → 数据库/缓存）

## 1.1 **硬件与底层表示（计算机组成原理）**

**目标**：理解数据在最底层是怎么存的。

- **物理存储介质**
    - **寄存器**（CPU 内部，最快）
    - **缓存 Cache**（L1/L2/L3，SRAM）
    - **主存 RAM**（DRAM，易失）
    - **磁盘 / SSD**（持久化，速度慢）
    - **其他存储**（显存、ROM、外设存储）
        
- **数据表示**
    - 二进制（0/1，逻辑电路开关）
    - 编码：原码、反码、补码（整数有符号存储）
    - 浮点数 IEEE 754
    - 字符编码（ASCII、UTF-8、UTF-16）
        
- **关键思想**：所有高层数据结构最终都要映射到二进制位。
    

---

## 1.2 **操作系统对存储的管理**

**目标**：理解 OS 如何抽象和管理物理存储。

- **内存管理**
    - 虚拟内存（地址空间抽象，MMU 翻译虚拟地址 → 物理地址）
    - 页表、分页/分段
    - 内存分配（堆、栈、全局区）
        
- **文件系统**
    - 磁盘文件的存储结构（inode、目录、块）
    - 缓存/缓冲区（Page Cache）
        
- **进程与线程**
    - 每个进程独立虚拟地址空间
    - 上下文切换时保存寄存器/栈信息
        
- **I/O 管理**
    - 缓冲 I/O、DMA、磁盘调度

---

## 1.3 **编程语言层面的存储模型**

**目标**：理解程序运行时的数据存储位置和生命周期。

- **基本类型的存储**
    - int、float、char 在内存中的布局（与补码/IEEE 754 联系）
        
- **数据结构的存储**
    - 数组（连续内存）、链表（节点分布在堆上）
    - 对象（Java 对象头 + 字段布局）
        
- **运行时内存划分**
    - **栈内存**：方法调用、局部变量
    - **堆内存**：动态分配对象（Java new、C malloc）
    - **方法区/元空间**：类元数据（Java）
        
- **内存管理**
    - Java GC（分代回收）
    - C 手动释放
        
- **序列化**
    - 对象在内存中 → 字节流（文件、网络传输）
    - 反序列化：字节流 → 内存对象
        

---

## 1.4 **数据库存储与缓存**

**目标**：理解高层数据系统如何落地存储。

- **关系型数据库（如 MySQL 等）**
    - 数据页（Page）和索引（B+ 树）
    - 缓存（Buffer Pool）
        
- **NoSQL 数据库**
    - 键值、文档、列式存储
    - **缓存数据库（Redis）**
        - 内存存储（dict、skiplist、listpack）
        - 持久化（RDB 快照、AOF 日志）
        - 序列化方式（RDB 文件格式）

---

## 1.5 学习路线建议

为了避免混乱，可以从底向上学，每一层都和上层对应起来：

1. **计算机组成原理**  
    二进制、补码、浮点数、寄存器、内存、磁盘。
    
2. **操作系统**  
    虚拟内存、进程地址空间、文件系统、内存分配。
    
3. **编程语言运行时**  
    Java 内存模型（JVM 堆、栈、方法区）、GC、对象布局。
    
4. **数据库/缓存实现原理**  
    数据结构存储方式、持久化机制、序列化。
    

---

## 1.6 一张整体脑图（结构化记忆）

```
存储体系
├── 硬件层
│   ├── CPU寄存器
│   ├── Cache
│   ├── RAM
│   ├── 磁盘/SSD
│   └── 数据表示(补码/IEEE754/字符编码)
├── 操作系统层
│   ├── 虚拟内存
│   ├── 内存分配(堆/栈)
│   ├── 文件系统(inode/块)
│   └── I/O缓冲区
├── 语言运行时层(Java)
│   ├── 基本类型存储
│   ├── 对象内存布局
│   ├── JVM堆/栈/方法区
│   ├── GC
│   └── 序列化与反序列化
└── 数据库与缓存
    ├── MySQL(页/索引/Buffer Pool)
    ├── Redis(内存结构/持久化)
    └── NoSQL存储模型
```

# 2 纵向串联

> 这里列举几个场景进行说明，无法涵盖所有
## 2.1 Java中数据存储

> 这部分可以结合[[JVM内存模型|JVM内存模型]]来看
### 2.1.1 示例代码

我们先准备一个尽可能涵盖各种数据的 Java 例子：

```java
public class StorageExample {

    // 静态变量（类变量）
    static int staticVar = 42;

    // 常量（编译期常量）
    static final String CONST = "Hello";

    public static void main(String[] args) {
        // 基本类型（局部变量）
        int localInt = 5;
        double localDouble = 3.14;

        // 引用类型（对象）
        Person person = new Person("Alice", 20);

        // 数组
        int[] numbers = {1, 2, 3};

        // 调用方法传参
        printPerson(person);
    }

    static void printPerson(Person p) {
        System.out.println(p.name + " - " + p.age);
    }
}

class Person {
    String name;
    int age;
    Person(String name, int age) {
        this.name = name;
        this.age = age;
    }
}
```

---

### 2.1.2 Java 内存结构视角

Java 程序运行时，JVM 会为不同种类的数据分配到不同的区域：

| 数据种类             | 存储位置                   | 生命周期   | 跨层关联                        |
| ---------------- | ---------------------- | ------ | --------------------------- |
| **局部变量（基本类型）**   | 栈（Stack Frame）         | 方法调用期间 | 栈虚拟地址 → OS 虚拟内存 → RAM（物理内存） |
| **局部变量（引用类型引用）** | 栈（存引用）                 | 方法调用期间 | 栈中存对象地址，指向堆                 |
| **对象实例**         | 堆（Heap）                | GC 管理  | 堆虚拟地址 → OS 虚拟内存 → RAM       |
| **数组**           | 堆（Heap）                | GC 管理  | 同上                          |
| **类变量（static）**  | 方法区（Metaspace 中的静态变量区） | 类加载到卸载 | OS 虚拟内存 → RAM               |
| **常量池数据**        | 方法区（运行时常量池）            | 类加载到卸载 | 同上                          |
| **方法字节码**        | 方法区                    | 类加载到卸载 | 同上                          |

---

### 2.1.3 代码中各数据的存储流程

#### 2.1.3.1 **基本类型（局部变量）**

```java
int localInt = 5;
double localDouble = 3.14;
```

- **JVM 栈帧**：
    
    - `localInt` 占用 4 字节，存==补码==形式 `0000 0000 0000 0000 0000 0000 0000 0101`
    - `localDouble` 占用 8 字节，存 IEEE 754 格式
        
- **跨层映射**：  
    栈（虚拟地址）→ OS 页表映射 → 物理内存（DRAM）→ CPU 寄存器加载/计算
    

---

#### 2.1.3.2 **引用类型**

```java
Person person = new Person("Alice", 20);
```

- **栈**：变量 `person` 存储一个引用（对象在堆的内存地址，即偏移位置）
    
- **堆**：存放 `Person` 对象实例
    
    - 对象头（Mark Word + 类型指针）
    - `name` 字段（引用 → 指向字符串对象）
    - `age` 字段（补码形式的 int）
        
- **跨层映射**：  
    栈中引用 → 堆内存（虚拟地址）→ 页表 → DRAM → CPU 访问
	
-  ==**函数结束后的情况**==
	- **栈帧销毁**
	    - 当 `foo()` 执行完毕，栈帧被弹出，局部变量（包括 `arr` 的引用）消失
	    - **此时你再也没有指向这个数组的引用了**
	- **对象本身（在堆里）并不会立刻销毁**
	    - 因为 Java 中 **对象的生命周期由 GC 决定**，而不是函数结束就清理
	    - 如果对象变成**不可达（unreachable）**，它会在下一次 GC 扫描时被回收
	- **可能的结果**：
	    - 如果 GC 很快运行，这个对象会很快被回收
	    - 如果 GC 没有立刻触发，这个对象依然会占用内存（但不会被访问）

---

#### 2.1.3.3 **数组**

```java
int[] numbers = {1, 2, 3};
```

- **栈**：`numbers` 存引用（数组对象的地址）
    
- **堆**：数组对象
    - 对象头（数组长度等元数据）
    - 元素区：3 个 int，每个用补码存储
        
- **跨层映射**：  
    和对象相同，访问数组需要经过栈引用 → 堆对象
    

---

#### 2.1.3.4 **静态变量**

```java
static int staticVar = 42;
```

- **方法区（Metaspace）**：  
    存放在类加载时分配的静态变量区
- **跨层映射**：  
    方法区是 JVM 虚拟内存的一部分 → OS 页表 → RAM

---

#### 2.1.3.5 **编译期常量**

```java
static final String CONST = "Hello";
```

- **字符串字面量** `"Hello"` 会在编译时进入 **class 文件常量池**
- 类加载时常量池内容加载到 **运行时常量池（方法区）**
- `"Hello"` 对象存放在堆中，但其引用在常量池中
- **跨层映射**：  
    常量池（虚拟内存）→ 堆中的对象

---

#### 2.1.3.6 **方法调用参数**

```java
printPerson(person);
```

- JVM 栈帧中为 `printPerson` 方法创建新的局部变量表
- `p` 存引用（指向同一个堆对象）
- 对象没有被复制，只是多了一个引用

---

### 2.1.4 跨层完整映射（以 `Person person` 为例）

1. **Java 代码**  
    `Person person = new Person("Alice", 20);`
2. **JVM 层**
    - 栈变量 `person`：引用（对象地址）
    - 堆对象：对象头 + 字段（`name` 引用 → String 对象，`age` int 值）
3. **OS 层**
    - JVM 堆的虚拟地址空间分配一块区域
    - 页表映射虚拟地址 → 物理内存页
4. **硬件层**
    - DRAM 存储对象内容的二进制位
    - CPU 通过寄存器 + 总线访问 DRAM

---

### 2.1.5 总结思路

- **基本类型**：直接在栈或堆中以二进制存储（补码 / IEEE 754）
- **引用类型**：栈存地址，堆存内容
- **数组**：是对象，连续内存
- **静态变量**：方法区
- **常量**：方法区常量池（引用） + 堆（对象）
- **跨层本质**：  
    Java 变量 → JVM 虚拟内存（堆/栈/方法区）→ OS 虚拟内存 → 页表映射 → 物理内存 → CPU 寄存器访问
---

## 2.2 Java中数据取用
### 2.2.1 先用一个例子

```java
public class StorageExample {
    public static void main(String[] args) {
        int x = 5;                   // 基本类型
        Person p = new Person(20);   // 引用类型
        int age = p.age;             // 使用对象字段
        System.out.println(age);     // 打印
    }
}

class Person {
    int age;
    Person(int age) { this.age = age; }
}
```

---

### 2.2.2 **数据取用的分步骤过程**

#### 2.2.2.1 **基本类型（x = 5）**

- **编译阶段**  
    `5` 转成补码 `0000...0101`，编译器在字节码中用 `iconst_5` 指令表示。
    
- **运行时取用**
    
    1. **JVM 解释执行** `iconst_5` → 把常量 5 压入操作数栈（位于当前栈帧）
    2. **局部变量表** 中存储这个值（在栈上）
    3. CPU 在执行字节码时会把这个栈变量的值加载到寄存器中运算（如加减）。
        
- **跨层路径**  
    栈内存（虚拟地址） → OS 页表 → 物理内存（RAM） → CPU 寄存器
    

---

#### 2.2.2.2 **引用类型（p = new Person(20)）**

- **存储阶段**
    
    - `p` 变量在栈中，存一个 **对象引用地址**（指向堆）
    - `Person` 对象在堆中，`age` 字段存补码形式的 int 值 20
        
- **取用 `p.age` 时**
    
    1. 栈中读取 `p` 的引用值（这是一个虚拟地址）
    2. JVM 在堆中根据引用找到对象起始地址
    3. 根据对象布局偏移量（`age` 字段在对象中的位置）找到对应 4 字节
    4. 从堆内存中读取 4 字节到 CPU 寄存器
        
- **跨层路径**  
    栈（引用） → 堆（字段值） → OS 页表映射 → RAM → CPU
    

---

#### 2.2.2.3 **常量/静态变量**

- 如果是静态变量，引用在 **方法区**（Metaspace 的静态变量区）
    
- 取用时步骤：
    
    1. JVM 找到该类的运行时数据结构（Class 对象）
    2. 定位到静态变量所在的内存区域
    3. 如果是引用类型，再跳到堆取对象内容
    4. CPU 读入寄存器
        
- **跨层路径**  
    方法区（虚拟内存） → 页表映射 → RAM → CPU
    

---

### 2.2.3 **打印到控制台**

- `System.out.println(age)`
    - `age` 的值已经在寄存器中（或栈的操作数栈顶）
    - 调用 `PrintStream.println` 时，值会被转换成字符串（在堆中分配内存）
    - 最终写入标准输出流（I/O 系统调用 → OS 缓冲区 → 设备）
        

---

### 2.2.4 **取用数据的核心过程**

无论是基本类型还是引用类型，**取用时都会经历以下步骤**：

1. **找到变量的位置**（栈、堆、方法区）
2. **如果是引用** → 先取引用，再根据地址找到实际数据
3. **通过 OS 页表把虚拟地址映射到物理地址**
4. **从物理内存读到 CPU 寄存器**
5. **CPU 执行运算/传递结果**
6. **结果可能再次写回内存（更新变量）**

---

### 2.2.5 跨层“数据取用”示意链路

以 `p.age` 为例：

```
[Java 代码] p.age
    ↓
[JVM 层] 栈取引用 p → 堆取对象字段
    ↓
[OS 层] 页表映射虚拟地址 → 物理内存地址
    ↓
[硬件层] DRAM 读取数据 → CPU 寄存器
    ↓
[执行] 参与运算/输出
```

---

### 2.2.6 关键补充

- **基本类型** → 直接读栈或堆的值
- **引用类型** → 先找引用，再访问堆数据
- **OS 页表** 保证不同进程的虚拟地址不会冲突
- **CPU 缓存**（L1/L2/L3 Cache）会让数据读取比直接访问 DRAM 快很多
- **GC** 可能会移动堆对象位置（压缩整理），引用会自动更新（JVM 层保证）

## 2.3 redis相关

### 2.3.1 逻辑层：你看到的数据类型

Redis 提供的常见数据结构：

- **String**（简单字符串、数字）
- **List**（双端链表）
- **Set**（无序集合）
- **Sorted Set**（带分数的有序集合）
- **Hash**（键值对映射）
- **Bitmap**、**HyperLogLog**、**Stream** 等

在**命令层**（例如 `SET key value`、`HSET key field value`），你会感觉 Redis “支持很多类型”，因为 API 不同、功能不同。

### 2.3.2 内部实现层：一切都是字节数组（sds）

- Redis 的 **最底层数据存储单元** 是 SDS（Simple Dynamic String），它本质是一个带长度字段的**字节数组**：
    
    ```c
    struct sdshdr {
        int len;     // 数据长度
        int free;    // 已分配但未使用空间
        char buf[];  // 实际存储的数据（字节序列）
    }
    ```
    
- 无论是字符串 `"abc"`，还是数字 `123`，甚至是序列化后的 JSON 或 protobuf 数据，最终都会存在 **`char[]`** 里。
- “数据类型”的差异，主要体现在：
    1. **逻辑解析方式不同**（同样的字节数组，List 和 Set 的解析规则不一样）
    2. **编码方式不同**（list 有 quicklist 编码，set 有 intset/hashtable 编码）

换句话说，**Redis 是“数据类型抽象 + 底层统一存储”的组合**。

- 不像 Java 或 MySQL，它不会在存储层**强制**“这是 int/这是 float”，而是：
    - **逻辑层**用一个“类型”来指导如何操作这些字节（比如做数值运算还是做字符串拼接）
    - **存储层**统一用 SDS 保存，这样不管你是 `String`、`Hash` 还是 `List`，里面的实际数据单位全是 SDS。

这样做的好处是：

1. **统一存储结构**，简化实现和内存管理。
2. **支持任意二进制**，比如直接存图片、压缩包。
3. **扩展灵活**，新增数据类型时只需组合/封装已有的 SDS 结构。

---

### 2.3.3 数据存储的纵向流程

假设你在 Java 中执行：

```java
String key = "user:1";
User user = new User("Alice", 25);
String json = serializeToJson(user);
jedis.set(key, json);
```

整个过程跨层拆开是这样的：

#### 2.3.3.1 应用层

- Java 对象 `user` 通过序列化方式（如 JSON、JDK 序列化、Protobuf）变成 **字节数组**（byte[]）
- 这是因为网络传输的单位是字节流，Redis 也只接收二进制
- ==注意这里无论是使用redis-cli还是使用比如Jedis这些客户端，都会进行序列化，转成字节数组。redis服务器只接收二进制==
#### 2.3.3.2 网络层

- 这个字节数组被打包成 Redis 协议（RESP）格式：
    
    ```
    *3\r\n$3\r\nSET\r\n$6\r\nuser:1\r\n$20\r\n{"name":"Alice","age":25}\r\n
    ```
    
- 通过 TCP 发送到 Redis 服务器
#### 2.3.3.3 Redis 协议解析

- Redis 读取 TCP 数据流，按 RESP 协议解析出命令和参数
- 解析后的参数（key、value）都是字节数组（sds 结构）

#### 2.3.3.4 内存存储

- Redis 把 key 存在一个全局哈希表（dict）中，dict 的键和值都是指向 SDS 的指针
- 如果是 String 类型，就直接是一个 SDS
- 如果是 List/Set/Hash 等类型：
    - Redis 会创建对应的结构（如 quicklist、hashtable）
    - 但这些结构里的每个元素依然是 SDS（即字节数组）

---

### 2.3.4 数据取出的纵向流程

假设 Java 执行：

```java
String json = jedis.get("user:1");
User user = parseFromJson(json);
```

#### 2.3.4.1 客户端发送 GET 命令

- `GET user:1` 经过 RESP 编码，通过 TCP 发送到 Redis

#### 2.3.4.2 Redis 查找

- Redis 根据 key 的哈希值在 dict 里找到对应的值（SDS）
- 直接返回 SDS 中的字节数组

#### 2.3.4.3 网络返回

- Redis 将 SDS 内容封装成 RESP 协议返回
- 客户端 TCP 接收，解析为字节数组

#### 2.3.4.4 应用层反序列化

- Java 把字节数组按 UTF-8 转成字符串（如果是 JSON）
- 再用 JSON 解析器反序列化成 `User` 对象

### 2.3.5 总结

```
Java 对象
  ↓（序列化）
字节数组（byte[]）
  ↓（RESP 协议）
TCP 数据流
  ↓（Redis 协议解析）
SDS（字节数组）
  ↓（dict、quicklist、hashtable等封装）
Redis 内存
```

取用是反向过程，只是最后一步要**反序列化**才能回到 Java 对象。

# 3 关于“字节”

## 3.1 不同类型的数据本质上都是“比特（bit）”的排列

- 计算机内存和存储介质的最小存储单位是 **比特（bit）**，即 0 或 1。
- 所有数据（整数、浮点数、字符、图片、音频、视频……）最终都以比特流的形式存在。
- 比特是最基本的存储单位，**比特是抽象单位，无法单独寻址**。

---

## 3.2 为什么说“字节（byte）”是基本寻址单位？

- **字节 = 8 个比特**，是计算机内存和外部设备（硬盘、网络包）寻址的最小单位。
- 现代计算机的内存、寄存器、总线基本上是以字节为单位进行寻址和传输的。
- 因此，所有数据都会按照字节对齐存储（对齐规则可能因架构而异）。
- ==所有更高层的数据（int、float、string、对象、图片等）在内存里都是某种 **字节序列**（byte array）==
	- 不管是 Java 的 `String`，还是 MySQL 的 `VARCHAR`，还是 Redis 的 `String` 类型，最终在底层都会落到某一段连续的字节上。
	- 差别只是：**上层解释这些字节的规则不同**。

---

## 3.3 不同数据类型如何映射到字节？

- **整数类型（int、long 等）**
    - 直接以补码形式编码为固定长度的字节序列
    - 例如 `int` 通常占 4 字节（32 bit）
- **浮点类型（float、double）**
    - 按 IEEE 754 标准编码成字节序列
- **字符类型（char）**
    - 本质上是数字编码（例如 UTF-16 码点），存储为 2 字节（Java 中 char 占 2 字节）
- **字符串**
    - 是字符的序列，存储时会用某种**字符编码**将字符转成字节序列（如 UTF-8、UTF-16、GBK 等）
- **复杂数据**
    - 图片、音频、视频、序列化对象等都是比特的组合，组织成字节数组

---

## 3.4 字符集（字符编码）和数据存储的关系

- **字符集**定义了“字符”与“数字代码点”的映射（比如 'A' 对应 Unicode 码点 U+0041）
- **字符编码**定义了“码点”如何编码成字节序列，常见编码有：
    - ASCII：单字节，兼容英语字符
    - UTF-8：可变长编码，兼容 ASCII，国际通用
    - UTF-16：固定或变长编码（Java 内部用的是 UTF-16 编码）
    - GBK、ISO-8859-1 等
- 存储字符串时，程序先把字符用字符编码转成字节序列，字节序列再写入内存或磁盘

---

## 3.5 **举例说明**

- 字符 `'你'`
    - Unicode 码点是 U+4F60
    - UTF-8 编码为 3 字节：`E4 BD A0`（11100100 10111101 10100000）
    - UTF-16 编码为 2 字节：`4F 60`
- 内存中是这几个字节的连续排列（对应比特流）

---
## 3.6 **总结**

|层级|说明|
|---|---|
|**比特（bit）**|最小存储单位，0 或 1|
|**字节（byte）**|8 个比特，内存寻址和数据传输最小单位|
|**数据类型**|各种类型按一定格式编码为字节序列（整型补码、浮点、字符串编码等）|
|**字符编码**|将字符映射到字节序列的规则（UTF-8、UTF-16 等）|

# 4 字节和字符
## 4.1 字符（Character）是什么？

- **字符**是人类语言的最小书写单位，是抽象的符号概念，比如字母 `A`，汉字 `你`，数字 `1`，标点符号 `，` 等。
- 字符本身是**抽象的符号**，不是具体的物理数据，不能直接存储或传输。
- 在计算机内，字符需要**映射为某个数值（码点）**，才能进行处理。

---

## 4.2 码点（Code Point）和字符集

- **码点**是字符在某个字符集（编码标准）中的唯一数字标识。
- 例如，Unicode 是目前主流的字符集标准：
    - `A` 的码点是 `U+0041`（十六进制）
    - `你` 的码点是 `U+4F60`
- 码点是字符的数字表示，用数字表示字符的唯一身份。

---

## 4.3 字符与字节的关系：字符编码（Character Encoding）

- **字符编码**是将字符的码点映射为字节序列的规则。
- 不同编码规则导致同一字符对应不同的字节表现形式。

### 4.3.1 常见编码示例：

| 编码方式   | 描述           | 例子（字符“你”）           |
| ------ | ------------ | ------------------- |
| ASCII  | 7位编码，英文字符    | 只支持英文字母、数字、符号等      |
| UTF-8  | 可变长编码，1~4字节  | `你` 编码为3字节：E4 BD A0 |
| UTF-16 | 2或4字节编码      | `你` 编码为2字节：4F 60    |
| GBK    | 中文扩展编码，1或2字节 | `你` 编码为2字节：C4 E3    |

---

## 4.4 字符和字节在计算机中的处理过程

1. **字符输入阶段**
    - 用户输入的是字符（抽象概念）
2. **字符编码阶段**
    - 字符通过编码转换成字节序列
3. **字节存储和传输阶段**
    - 计算机存储或传输字节序列
4. **字节解码阶段**
    - 接收端根据字符编码把字节序列还原成字符码点
5. **字符显示阶段**
    - 根据码点查字体渲染出可见字符

---

## 4.5 **举例说明**

- 字符 `'你'`
    - Unicode 码点是 U+4F60
    - UTF-8 编码为 3 字节：`E4 BD A0`（11100100 10111101 10100000）
    - UTF-16 编码为 2 字节：`4F 60`
- 内存中是这几个字节的连续排列（对应比特流）

## 4.6 常见问题举例

- **字符串长度**：
    - 用字符数衡量时是字符个数
    - 用字节数衡量时是所占字节数（不同编码不同）
- **截取字符串**：
    - 按字符截取不会破坏编码
    - 按字节截取可能切断多字节字符，导致乱码
---

## 4.7 Java 中的字符和字节

- Java 中 `char` 类型是 **16位（2字节）**，采用 UTF-16 编码表示单个字符码元（code unit）
- Java 的 `String` 内部是 UTF-16 编码的 `char` 数组
- `String.getBytes()` 方法可以把字符串按指定编码转成字节数组
- 编码转换是程序员手动控制的关键点，避免乱码

---

## 4.8 MySQL中的字符和字节

- 在 MySQL 里 **`CHAR(x)` 等中的 `x` 是“字符数”，不是字节数**，但实际占用的存储空间还是由**字符集**决定的。
## 4.9 总结

| 概念   | 定义             | 作用           |
| ---- | -------------- | ------------ |
| 字符   | 抽象符号（文字、数字、符号） | 表示人类可识别的信息单位 |
| 码点   | 字符在字符集中的数字标识   | 连接字符和数字的桥梁   |
| 字符编码 | 码点映射成字节序列的规则   | 字符与字节之间转换的标准 |
| 字节   | 存储单位，8比特       | 物理存储和传输的基本单位 |

## 4.10 不同语言中String获取某个下标对比

### 4.10.1 Java

- **字符串底层编码**：UTF-16（`char` 是16位码元）
- **下标访问**：
    - `String.charAt(int index)` 返回的是**第 index 个 UTF-16 码元（code unit）**，即一个 `char`
    - 注意：
        - 由于 UTF-16 中有“代理对”编码，某些 Unicode 字符（如 Emoji、部分生僻字）由两个 `char` 组成
        - 所以 `charAt` 并不总是对应一个完整的 Unicode 字符（code point）
            
- **总结**：下标访问是“码元”（16位单位），不完全等同于“字符”（Unicode 码点）

---

### 4.10.2 Python（3.x）

- **字符串内部编码**：
    - Python 3 中 `str` 是 Unicode 字符串
    - 底层具体实现依赖 Python 版本和编译环境，可能是 UTF-8、UTF-16 或 UTF-32，但对用户透明
- **下标访问**：
    - `s[index]` 返回的是**完整的 Unicode 字符（code point）**
    - Python 保证字符串的下标是基于字符（code point）而非字节或码元
- **总结**：访问是“字符”，对用户友好，隐藏了底层编码复杂性
    
---

### 4.10.3 JavaScript

- **字符串内部编码**：UTF-16 编码
- **下标访问**：
    - `str[index]` 返回的是 UTF-16 码元（16位单位），即一个码元
    - 和 Java 类似，对于代理对字符，会分成两个部分返回
- **补充**：
    - 可以用 `String.codePointAt(index)` 获取完整的 Unicode 码点
- **总结**：默认访问码元，不是完整字符
    
---

### 4.10.4 C / C++

- **字符串类型**：
    - C 中传统字符串是以 `char*` 存储的字节序列（ASCII 或其他编码）
    - C++ `std::string` 也是字节序列容器
- **下标访问**：
    - 访问的是**字节**（`char` 即1字节）
    - 不支持直接按字符访问（多字节字符需要借助库处理）
- **总结**：默认按字节访问，不支持多字节字符的直接访问

---

### 4.10.5 **总结对比表**

| 语言         | 字符串底层编码     | 下标访问类型          | 是否访问完整字符 | 备注            |
| ---------- | ----------- | --------------- | -------- | ------------- |
| Java       | UTF-16      | UTF-16 码元（char） | 否        | 代理对字符占两个下标    |
| Python 3   | Unicode（隐藏） | 完整 Unicode 字符   | 是        |               |
| JavaScript | UTF-16      | UTF-16 码元       | 否        | 有代理对同 Java    |
| C / C++    | 字节序列        | 字节              | 否        | 需额外库支持多字节字符处理 |
