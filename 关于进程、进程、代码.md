- 代码本质是“命令”，线程是最小的执行单元
	- **代码（程序）是静态的指令集合**，它本身没有执行能力，只有被执行时才有意义。
	- **线程是程序运行时的执行实体**，它顺序读取代码中的指令（命令），并执行对应的操作。
	- 线程在操作系统中由线程调度器管理，多个线程并发执行同一程序的不同部分。

- 代码是“死”的，线程根据代码执行命令做出行动
	- 代码是静态文本或字节码，无意识，无执行力。
	- 只有当线程（或进程）按照代码指令调度时，程序才能产生动态行为。
	- 线程的执行依赖CPU、内存等硬件资源的支持，代码执行的“生命”由线程赋予。

- 之前unity带来了错误的理解。**对象、实例和线程不是同层次的东西**，到agent就是更高层次的抽象，不要混为一谈，各层单独学习
	- **对象/实例**是程序中的数据结构或实体，属于程序语义层面。
	- **线程**是操作系统级别的执行单元，属于系统执行层面。
	- 两者分属不同抽象层：对象属于程序运行时的内存状态，线程则是使程序得以运行的实体。
	- 不同线程可以操作同一个对象，但对象本身不“执行”代码，线程才是执行者。

- 再往下一层，到“如何找到需要的代码（命令）”，就涉及到操作系统（如何把所需加载到内存中，如何管理内存，虚拟内存，文件系统等），还有计组（如何寻址，整个计算机模型相关），亦或者到数电

- **“如何找到需要的代码（命令）”涉及操作系统与计算机组成原理**
	
	- **操作系统**负责文件管理、进程/线程管理和内存管理，包括虚拟内存机制和文件系统。
	- 当线程执行代码时，操作系统协助从磁盘加载程序代码到内存，并管理这些内存页的映射与访问权限。
	- **计算机组成原理（计组）涵盖CPU寻址、指令集架构、存储体系结构（寄存器、缓存、主存等）和总线通信等，保障指令能够被正确寻址和执行。
	- 数字电路层面（数电）则是底层硬件逻辑，实现存储器、算术逻辑单元、控制器等核心部件。
	- 补充：程序的执行路径从“高层代码” → “操作系统加载和调度” → “硬件指令执行”，这是一个层层递进的过程。


> 其实学校的整个学习就是从最底层依次网上学习，从数电，到计组，到操作系统，层次逐渐往上
> 感觉以后有空还是要把这些知识重新梳理学习，或许会有更多理解

> 但其实每一层也都是独立的，但要能够分清楚自己现在学的属于哪一层