八股：

1. Java中的优先队列了解吗？各项操作的时间复杂度，比如插入删除？
2. DFS和BFS的区别？
3. 对于系统内存上的栈内存和堆内存有什么区别？为什么性能上会有区别？
4. 物理内存和虚拟内存的区别和关联？
5. 进程和线程的区别？
6. 进程间通信和线程间通信都有哪些方式？
7. Java的GC了解多少？内存管理的具体原理？
8. Java中线程池的基本原理？
9. 整个线程池设计时，核心线程数量和最大线程数量是根据什么来定？
10. 线程池中的队列大小根据什么进行设计？
11. 那按照你原有设计，如果是多个队列，多个队列之间，如果任务都塞到某一个队列，因为你有多个线程，可能每个线程处理速度不一样，有些处理很快，有些处理很慢，如何保证线程池的负载均衡？如果有些线程处理很快空闲下来，但其他线程处理慢，整体其实会变慢。对于这种情况，有没有考虑过怎么设计？

项目：（简历太烂了纯挖坑）

1. 两个项目是什么性质的（学校or兴趣）
2. 后端用了什么数据结构和算法优化？（双数据库）相比传统的数据结构，为什么你这个设计结构能起到优化效果？
3. 设计完成后，原来也有一套设计，你这个设计比原来那个响应速度和准确性有没有量化的数据提升？具体提高了多少？  没有做相关测试，没有具体数据去展示，那怎么能说明你设计的系统，或者你刚才说的数据拆分、分表等优化是有效果的？  
4. 或者说从哪些维度你能分析，得出采用这些优化后性能提高了，而且没有带来副作用？
5. 那你改之后对整体系统内存有影响吗？ 有没有实际测试和评估？那怎么确认内存会有额外开销？原因是什么？
6. 主要的难点?
7. 虽然你讲了很多系统设计的提升，那系统里面最大的问题是什么？
8. 你的消息队列大小怎么设计？如何满足最高并发？
9. 聊天机器人是直接用 OpenAI API 还是做了一层封装？能介绍一下功能吗？  
10. 记忆池设计？
11. 除了调用 API，你还做了什么？
12. 怎么适配多个不同的大模型API
13. 使用多个模型API，网络负载和各方面会不会有压力？  
14. Deepseek在线使用的时候其实它本来就有点卡，或者有点慢，有时候回复不了。那如果出现这种情况，你这里怎么去处理？有可能他API返回给你就是错的？
15. 那这里我看你写的那个三级缓存的三级结构。那这里的话内存的话最大会到多大？会一直膨胀吗？压缩解压缩的过程？
16. 整体包的体积会多大？代码量多大？
17. 有使用什么设计模式吗？（单例）
18. 工厂模式、装饰器模式等其他模式有学习过吗？
19. 继承和多态的基础的原理是什么？有没有了解过底层实现的一些原理？
20. 对于Java上的协程有没有使用过？
21. 把IO密集的提取出来用协程？

手撕：

```java
/**
 * 给你一个字符串 s 和一个整数 k ，请你找出 至多 包含 k 个 不同 字符的最长子串，并返回该子串的长度。
 *
 * 示例 1：
 * 输入：s = "eceba", k = 2
 * 输出：3
 * 解释：满足题目要求的子串是 "ece" ，长度为 3 。
 * 示例 2：
 * 输入：s = ""abaccc"", k = 2
 * 输出：4
 * 解释：满足题目要求的子串是 "accc" ，长度为 4 。
 * 提示：
 * •    1 <= s.length <= 5 * 104
 * •    0 <= k <= 50
 * •    算法时间复杂度<O(n^2)
 */
```

